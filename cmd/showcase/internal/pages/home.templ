package pages

import (
	"github.com/kinnsei/dsx/cmd/showcase/internal/layouts"
	"github.com/kinnsei/dsx/ui/markdown"
)

templ Home(readmeContent string) {
	@layouts.Showcase(layouts.ShowcaseProps{
		Title:       "dsx showcase",
		Description: "Go web framework with DaisyUI and Datastar",
		CurrentPath: "/",
	}) {
		@markdown.Markdown(markdown.Props{
			Content: readmeContent,
		})
	}
}
